/**
 * GitHub OAuth Authentication Configuration
 * Generated by create-karetech-stack
 */

export const githubAuthConfig = {
  // GitHub OAuth provider
  socialProviders: {
    github: {
      clientId: process.env.GITHUB_CLIENT_ID!,
      clientSecret: process.env.GITHUB_CLIENT_SECRET!,
      scope: ["user:email"], // Request user email access
    },
  },

  // Callback URL configuration
  callbacks: {
    signIn: "/dashboard", // Redirect after successful sign in
    signOut: "/", // Redirect after sign out
    error: "/auth/error", // Redirect on error
  },

  // Account linking settings
  account: {
    linkAccounts: true, // Allow linking GitHub to existing accounts
  },

  // User data mapping
  userMapping: {
    // Map GitHub user data to your user schema
    id: "id",
    name: "name",
    email: "email",
    image: "avatar_url",
    username: "login",
  },
};